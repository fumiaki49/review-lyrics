%h1.app-title Review Lyric
= render "shared/side-bar"

.main-container
  .postted-lyric
    .postted-lyric__list
      .postted-lyric__list__icon
        -# -@users.each do |user|
        -#   = image_tag(user.image.url, width: 50, height: 50)
        -#   = user.nickname
      .postted-lyric__list__song
        .postted-lyric__list__song__lyric
